# Configurations

This directory is reserved for SafeFlow-owned policies and experiment configuration files.

No policy file is included yet. Adding an automata policy belongs to a later implementation phase and must be reviewed independently from this workspace refactor.
